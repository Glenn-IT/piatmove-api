Standard form text field with a label above, optional leading/trailing icon, and a focus ring in brand blue. Set `error` + `helperText` for validation messages.

```jsx
<Input label="Mobile Number" placeholder="09xx xxx xxxx" leadingIcon={<Icon name="phone" />} required />
<Input label="Password" type="password" error helperText="Incorrect password" />
```
