Toggleable pill for single/multi selection — vehicle types on the booking form, list filters. Drives `selected` state from the parent.

```jsx
<Chip selected leadingIcon={<Icon name="car" />}>Sedan</Chip>
<Chip>Van</Chip>
```
