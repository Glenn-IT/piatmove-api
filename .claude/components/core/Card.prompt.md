White rounded surface that holds all card-based content. Use `interactive` for tappable cards (booking cards, list rows) to get a hover lift.

```jsx
<Card elevation="md" interactive>
  <h3>Active Booking</h3>
</Card>
```

`padding`: none|sm|md|lg. `elevation`: none|xs|sm|md.
