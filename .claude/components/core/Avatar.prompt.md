Circular avatar for passengers and drivers. Falls back to initials on a blue tint when `src` is absent. Add `status` for driver presence dots.

```jsx
<Avatar name="Maria Santos" src="/photo.jpg" size={48} status="online" />
```
