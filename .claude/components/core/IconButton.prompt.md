Icon-only square button for app-bar actions (back, notifications, menu). Always pass `label` for accessibility.

```jsx
<IconButton variant="soft" label="Notifications"><Icon name="bell" /></IconButton>
```
