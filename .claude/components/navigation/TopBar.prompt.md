Screen header. Use `variant="default"` on white screens, `variant="brand"` when it sits over a blue hero gradient (text turns white).

```jsx
<TopBar title="Book Vehicle" leading={<IconButton label="Back"><Icon name="arrow-left"/></IconButton>} />
```
