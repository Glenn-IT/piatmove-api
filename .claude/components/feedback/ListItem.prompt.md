Single row for notification feeds, trip lists, and settings menus. Compose with a `leading` icon tile or `Avatar`, optional `meta` timestamp, and a `trailing` chevron.

```jsx
<ListItem leading={<IconTile name="check"/>} title="Booking Approved" subtitle="Your trip to Tuguegarao is confirmed" meta="2m" />
```
